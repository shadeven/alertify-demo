var expect = chai.expect;

describe('Unit Tests', function() {
  it('check variable value', function() {
    var foo = 'bar';
    expect(foo).to.equal('bar');
  });

  it('check variable type', function() {

  });
});
